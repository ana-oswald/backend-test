{% extends 'base_app.html.twig' %}

{% block body %}
    <h1>{{ post.title }}</h1>
    <h5>{{ post.category.name }} on {{ post.publishDate|date('Y-m-d') }}</h5>

    <hr /><br />

    <p>{{ post.intro }}</p>

    <hr /><br />

    <h3>Post Details:</h3>
    {% for text in post.textContentList %}
        <h3>{{ text.title }}</h3>
        <p>{{ text.text }}</p>
    {% endfor %}
    <hr/><br />

    <h3>Comments:</h3>


    <div style="background: #cccccc; ">
        <p>Name: <a href="mailto:test@gmail.com">Thomas Faduncho</a></p>
        <p>First line with two spaces after.
            And the next line.

            First line with the HTML tag after.<br>
            And the next line.
        </p>
    </div>
    <hr />
    <div style="background: #cccccc; ">
        <p>Name: <a href="mailto:test@gmail.com">Jonhy Bravo</a></p>
        <p>First line with two spaces after.</p>
    </div>

{% endblock %}