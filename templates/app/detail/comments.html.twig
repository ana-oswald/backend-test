<h3>Comments:</h3>

{% for comment in post.comments %}
  <div style="background: #cccccc; ">
    <p>Name:
      <a href="mailto:{{ comment.email }}">{{ comment.name }}</a>
    </p>
    <p>{{ comment.comment }}
    </p>
  </div>
  <hr/>
{% endfor %}